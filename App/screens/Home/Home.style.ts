import {StyleSheet} from 'react-native';

import {black, black_30} from '@APPNAME/constants';

const styles = StyleSheet.create({
  button: {
    backgroundColor: black_30,
    borderColor: black,
    borderWidth: 2,
    marginVertical: 10,
  },
});

export default styles;
